"""Role agents that pivot the cached Profound snapshot into decisions + actions.

Each agent mirrors a Profound Agent Builder graph node-for-node so the UI build
is a transcription, not a re-design. The CMO agent is the orchestrator/router.
"""
